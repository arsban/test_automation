`git clone https://github.com/arsban/test_automation.git`

`cd test_automation`

`python -m venv venv`

`source venv/bin/activate`

`pip install --upgrade pip`

`pip install -r requirements`

`pytest test_browser.py`